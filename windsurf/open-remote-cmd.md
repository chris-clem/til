# Quickly open remote dirs in Windsurf

Access remote dirs in Windsurf with the following command:

```bash
windsurf --folder-uri vscode-remote://ssh-remote+<remote-host>/home/cuser/<path-to-repo>
```
